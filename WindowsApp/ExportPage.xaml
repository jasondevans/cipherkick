<local:StandardUserControl x:Class="PasswordManager.ExportPage" x:Name="exportPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordManager"
             mc:Ignorable="d">

    <Grid Margin="10,15,10,15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" x:Name="generateButton" Width="280" HorizontalAlignment="Left" Click="generateButton_Click" Style="{StaticResource MediumButtonStyle}" Content="generate export data"/>
        <TextBox Grid.Row="1" x:Name="exportDataTextBox" AcceptsReturn="True" Margin="0,10,0,0" FontSize="24" FontWeight="Bold" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"/>
    </Grid>

</local:StandardUserControl>
